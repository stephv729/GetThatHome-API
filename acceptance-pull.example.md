# Only these will be accepted

Add: new file to services/auth_services/js
Change: background color to components/LoginForm
Remove: ...to [PATH]
Fix: ...to [PATH]
Resolve: ...to [PATH]
Add feat (nombre del feature)
